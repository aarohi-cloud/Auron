# =============================================================
# requirements.txt — All Python dependencies
# Install with: pip install -r requirements.txt
# =============================================================

# --- Phase 1: Core AI & Config ---
anthropic>=0.40.0           # Anthropic Claude API SDK
pydantic>=2.7.0             # Data validation
pydantic-settings>=2.3.0    # Config from env vars
pyyaml>=6.0.1               # Read agent.yaml
jsonschema>=4.22.0          # Validate config schema
typer>=0.12.0               # CLI interface
rich>=13.7.0                # Pretty terminal output
python-dotenv>=1.0.1        # Read .env file
structlog>=24.2.0           # Structured JSON logging
anyio>=4.4.0                # Async utilities
pytest>=8.2.0               # Test runner
pytest-asyncio>=0.23.7      # Async test support

# --- Phase 2: Tools & Memory ---
duckduckgo-search>=6.2.0    # Free web search (no API key needed)
httpx>=0.27.0               # Async HTTP client (for read_url tool)
beautifulsoup4>=4.12.0      # HTML parser (strips tags from web pages)
chromadb>=0.5.0             # Vector database for persistent memory

# --- Phase 2: Optional (for Ollama / OpenAI-compatible APIs) ---
# Uncomment if you want to use Ollama or Groq:
# openai>=1.30.0            # OpenAI SDK (also used for Ollama/Groq)